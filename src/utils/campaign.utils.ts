function generateSubCampaignName(index: number) {
  return `Chiến dịch con ${index}`;
}

function generateAdvertisementName(index: number) {
  return `Quảng cáo ${index}`;
}

export { generateSubCampaignName, generateAdvertisementName };
